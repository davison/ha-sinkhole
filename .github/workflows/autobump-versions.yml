# .github/workflows/autobump-versions.yml
name: ⬆️ Autobump Image Versions

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions:
  contents: write
  pull-requests: read

jobs:
  autobump:
    name: 🤖 Bump Version
    runs-on: ubuntu-latest
    
    # This prevents the workflow from re-triggering itself.
    # It will only run if the event was triggered by a user,
    # not by the 'github-actions[bot]' that this workflow uses.
    if: github.actor != 'github-actions[bot]'

    steps:
      - name: ⬇️ Checkout PR Branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - name: ⚙️ Configure Git
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: 📦 Install Python Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install semver

      - name: ⚡ Run Version Bump Script
        id: bump_script
        run: |
          python .github/scripts/bump_versions.py
        env:
          BASE_REF: "origin/${{ github.base_ref }}"
          HEAD_REF: "origin/${{ github.head_ref }}"

      - name: ⬆️ Commit and Push Changes
        if: steps.bump_script.outputs.commit_made == 'true'
        run: |
          echo "Committing and pushing version bumps..."
          # Add all VERSION files that were modified
          git add ./**/VERSION
          git commit -m "chore: automatic version bumps"
          # Push to the PR branch (HEAD)
          git push origin HEAD:${{ github.head_ref }}
